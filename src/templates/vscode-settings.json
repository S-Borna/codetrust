{
  "//": "CodeTrust Agent Optimizer — VS Code workspace settings",
  "//generated": "by codetrust setup — https://codetrust.ai",
  "github.copilot.chat.useInstructionFiles": true,
  "chat.agent.instructions": [
    {
      "text": "CodeTrust governance is active. Call codetrust_validate_command before terminal commands, codetrust_validate_file_write before writing files, codetrust_validate_package before installing packages. Run codetrust_static_scan after changes. NEVER run git push."
    }
  ]
}
